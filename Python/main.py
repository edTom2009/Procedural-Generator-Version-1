#libraries
import random
import math
import numpy


#variables
WIDTH = 100 #placeholder values
HEIGHT = 100
DETAIL = 50
SEED = 0

#functions
def normalise(value, scale):
    normValue = value/scale
    return(normalValue)

if SEED == 0:

